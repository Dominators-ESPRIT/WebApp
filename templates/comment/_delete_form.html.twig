<form method="post" action="{{ path('comment_delete', {'idcomment': comment.idcomment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.idcomment) }}">
    <button class="btn">Delete</button>
</form>
