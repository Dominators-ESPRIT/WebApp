<form method="post" action="{{ path('oeuvre_competition_delete', {'id': oeuvre_competition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvre_competition.id) }}">
    <button class="btn">Delete</button>
</form>
